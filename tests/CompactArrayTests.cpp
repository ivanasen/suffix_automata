#include <iostream>
#include "../src/CompactArray.hpp"

using namespace std;

int main()
{
    // const int SIZE = 26;
    // CompactArray<SIZE> s;
    // for (int i = 0; i < SIZE; ++i)
    //     cout << (CompactArray<SIZE>::DEFAULT == s.get(i));

    // s.set(4, 3);
    // cout << (3 == s.get(4));
    // cout << (4 == s.id0);
    // cout << (1 == s.size);

    // s.set(4, 10);
    // cout << (10 == s.get(4));
    // cout << (4 == s.id0);
    // cout << (1 == s.size);

    // s.set(10, 23);
    // cout << (23 == s.get(10));
    // cout << (10 == s.id1);
    // cout << (2 == s.size);



    return 0;
}