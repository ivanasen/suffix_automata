{
    "tasks": [
        {
            "type": "shell",
            "label": "g++ build active file",
            "command": "/usr/bin/g++",
            "args": [
                "-g",
                "${workspaceFolder}/src/Main.cpp",
                "${workspaceFolder}/src/SuffixAutomata.cpp",
                "-o",
                "${workspaceFolder}/SuffixAutomata"
            ],
            "options": {
                "cwd": "/usr/bin"
            }
        }
    ],
    "version": "2.0.0"
}